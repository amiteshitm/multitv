import { uploadCreate } from 'server/graph/mutations/upload/create'
import featureCreate from 'server/graph/mutations/feature/create'

const mutations = {
  uploadCreate,
  featureCreate,
}

export default mutations
